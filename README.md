# YUAIDJs: Your User-Activated Interface by Dynamic JavaScript 🚀✨
**YUAIDJs** — это фреймворк-диджей, который миксует UI прямо в браузере! 

> 🚀 Быстрый.  
> 🧠 Реактивный.
> 📦 Надежный. 
> 🎉 Весёлый.  

### 1️⃣ YUAIDJs **не портит Lighthouse**

HTML пришёл с сервера → Lighthouse = **100/100**
JS-движок **не ломает**:

* FCP
* LCP
* TBT
* CLS

Это ключевое отличие от SPA-фреймворков.

---

### 2️⃣ Нулевой Total Blocking Time

**TBT = 0 ms** означает:

* нет тяжёлого JS-рантайма
* нет блокирующих reconcile/VDOM
* нет гидрации

Для React/Vue это почти недостижимо **даже на SSR**.

---

### 3️⃣ WebGL не влияет на CWV

WebGL и визуализация:

* не блокируют main thread при загрузке
* не ухудшают LCP
* не влияют на CLS

## ⚙️ Установка

```html
<script defer src="https://endlessdub.ru/yuaidjs.min.js"></script>
```

Теперь у тебя есть `window.YUAIDJs` — фреймворк на штурвале!

```js
const YUAIDJs = {
    modules: {}, // Хранилище динамически загружаемых модулей (animation, pwa и т.д.)
    activeAnimations: new Map(), // Активные requestAnimationFrame для плавных переходов
    _initialised: false, // Флаг: выполнена ли полная инициализация (защита от повторного init)
    _eventHandlersAttached: false, // Флаг: установлены ли глобальные обработчики click/submit/popstate
    _serviceWorkerRegistered: false, // Флаг: зарегистрирован ли Service Worker (PWA)

    config: {
        // === ОСНОВНЫЕ НАСТРОЙКИ ===
        debug: false, // true = логи в консоль + ошибки в #error-log на экран. false = только console.error критических ошибок
        devMode: false, // true = без .min.js + cache-bust (?t=NOW) + отключен Service Worker + очистка всех SW кэшей
        maxRetries: 3, // Максимум повторов fetch/updateByKey при сетевых ошибках (fetchPage, updateDOM)
        retryDelay: 1000, // Задержка между повторами в мс (по умолчанию 1 сек)
        
        // === CIRCUIT BREAKER (авто-убийство при фатальных ошибках) ===
        fatalErrors: 0, // Счетчик фатальных ошибок (fetch/updateByKey/dom/navigation)
        maxFatalErrors: 3, // После этого количества → this.kill() + location.reload()
        _circuitOpen: false, // Внутренний флаг: circuit breaker активирован (блокирует все updateByKey)
        _updateLock: false, // Блокировка параллельных updateByKey (debounce на уровне фреймворка)
        
        // === UI/НАВИГАЦИЯ ===
        defaultContainer: 'yuaidjs-app', // ID контейнера для navigateTo() без data-yuaidjs-update-key
        defaultTransition: 'fade', // Переход по умолчанию для updateDOM() (если animation модуль активен)
        
        // === ИНФРАСТРУКТУРА ===
        rpcEndpoint: 'wss://endlessdub.ru', // WebSocket для Service Worker (передается в SW при регистрации)
        
        // === МОДУЛИ (true = авто-загрузка /modules/{name}.js или .min.js) ===
        modules: {
            animation: true, // requestAnimationFrame + плавные переходы (fade/slide). Заменяет target.innerHTML
            auth: false, // localStorage + WebAuthn + JWT. Логин/регистрация + protected роуты
            speech: false, // Web Speech API (SpeechRecognition/Synthesis). Голосовой ввод/чтение
            radio: true, // Web Audio API + MediaSource. ICECAST/SHOUTcast стриминг + визуализатор
            video: false, // HTML5 Video + MSE (Media Source Extensions). HLS/DASH + PIP
            file: false, // File API + Drag-n-Drop + FileSystem API. Загрузка/предпросмотр файлов
            call: false, // WebRTC (getUserMedia + RTCPeerConnection). Видео/аудио звонки P2P
            screen: false, // getDisplayMedia(). Захват экрана/окна для стримов
            record: false, // MediaRecorder API. Запись аудио/видео + Blob export
            notify: false, // Notifications API + Vibration API. Пуш-уведомления + вибрация
            wasm: false, // WebAssembly.instantiate(). Загрузка/выполнение .wasm модулей (Rust/Go/C++)
            pwa: true, // Service Worker + Cache API + manifest.json. Оффлайн-режим + установка
            visualizer: true, // Web Audio API + Canvas 2D/WebGL. Аудио-визуализатор (FFT/спектр)
            tontelegram: false, // TON Web3 + Telegram Mini Apps API. Крипто-кошелек + Telegram виджеты
        }
    },

```

## 🛠️ Отладка

Включи флаг:

```js
debug = true;
```

...и получай логи прямо в панель `#yuai-debug` на экране 📈

---

## 🧚️ Совместимость и сравнение

| Фреймворк        | Lighthouse Perf* | DOM update | Память ≈ | SSR | CSR | WebSocket | Выключить фреймворк? | Самовосстановление |
|------------------|------------------|------------|----------|-----|-----|-----------|---------------------|--------------------|
| **YUAIDJs**      | **100/100**** 🎯 | **0.2–0.3ms** | **5MB** | Да  | Да  | 🟢 Нативный | ✅ Да — HTML без рантайма | ✅ Да (circuit + kill/revive) |
| Next.js (React)  | ≈ 95–99/100 | ~1.8–2.5ms | 45–55MB | Да | Да | ⚫ Через lib | ❌ Нельзя отключить runtime | ❌ Нет |
| Nuxt.js (Vue)    | ≈ 96–98/100 | ~1.2–1.8ms | 28–35MB | Да | Да | ⚫ Через lib | ❌ Нельзя отключить | ❌ Нет |
| SvelteKit        | ≈ 99/100 | ~0.4–0.6ms | 18–22MB | Да | Да | ⚫ Через lib | ❌ Частично только SSG | ❌ Нет |
| Angular          | ~80–85/100 | ~2.5–3.5ms | 55–65MB | Да | Да | ⚫ Через lib | ❌ Нельзя | ❌ Нет |
| Yew (Rust/WASM)  | n/a | ~3–5ms | 35–45MB (wasm + runtime) | ❌ Серверный рендер нет | Да | ⚫ Только вручную | ❌ Нельзя | ❌ Нет |
| Leptos (Rust)    | n/a | ~0.8–1.2ms | 22–28MB | Да | Да | 🟢 Да | ❌ Нельзя | ❌ Нет |

---



YUAIDJs — это компактный, реактивный и живой фреймворк для настоящих интерфейсных диджеев 🎧
Демо здесь:  [endlessdub.ru](https://endlessdub.ru/)
---

## 🤝 Лицензия

Свободный как ритм на пляже!

YUAIDJs — миксуй, расширяй и пускай интерфейс в пляс 🌟🎧
